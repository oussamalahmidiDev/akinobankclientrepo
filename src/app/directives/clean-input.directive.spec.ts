import { CleanInputDirective } from './clean-input.directive';

describe('CleanInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CleanInputDirective();
    expect(directive).toBeTruthy();
  });
});
